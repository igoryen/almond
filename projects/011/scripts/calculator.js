//calculator.js

// these are no good because they are global space named functions
/*
function add (x, y){
  return x + y;
}

function subtract (x, y){
  return x - y;
}
*/


var calculator = {}; // 1

// 3
/*
calculator.add = function (x, y){ // 10 , 12
  return x + y;
};

calculator.subtract = function (x, y){ // 20
  return x - y;
};

calculator.multiply = function (x, y){ // 20
  return x * y;
};

calculator.divide = function (x, y){ // 20
  return x / y;
};
*/
